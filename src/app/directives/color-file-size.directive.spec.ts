import { ColorFileSizeDirective } from './color-file-size.directive';

describe('ColorFileSizeDirective', () => {
  it('should create an instance', () => {
    const directive = new ColorFileSizeDirective();
    expect(directive).toBeTruthy();
  });
});
